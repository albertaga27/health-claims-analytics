# =============================================================================
# CLAIMS HEALTH ANALYTICS - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# Do not commit the .env file to version control
# =============================================================================

# =============================================================================
# AZURE OPENAI SERVICE (REQUIRED)
# =============================================================================
# Azure OpenAI endpoint URL (e.g., https://your-resource.openai.azure.com/)
AZURE_OPENAI_ENDPOINT=

# Model deployment name in Azure OpenAI Studio
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4.1-mini

# Azure OpenAI API key (optional if using Azure CLI authentication)
AZURE_OPENAI_API_KEY=

# =============================================================================
# AZURE TEXT ANALYTICS FOR HEALTH (OPTIONAL)
# =============================================================================
# Text Analytics endpoint URL (e.g., https://your-resource.cognitiveservices.azure.com/)
AZURE_TEXT_ANALYTICS_ENDPOINT=

# Text Analytics API key
AZURE_TEXT_ANALYTICS_KEY=

# =============================================================================
# AZURE DOCUMENT INTELLIGENCE (OPTIONAL)
# =============================================================================
# Document Intelligence endpoint URL (e.g., https://your-resource.cognitiveservices.azure.com/)
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=

# Document Intelligence API key
AZURE_DOCUMENT_INTELLIGENCE_KEY=

# =============================================================================
# AGENT MODEL CONFIGURATION (OPTIONAL OVERRIDES)
# =============================================================================
# Model used for document extraction (default: gpt-4.1-mini)
DOCUMENT_EXTRACTION_MODEL=gpt-4.1-mini

# Model used for health analytics (default: text-analytics-health)
HEALTH_ANALYTICS_MODEL=text-analytics-health

# Model used for underwriting reasoning (default: gpt-4.1)
REASONING_MODEL=gpt-4.1

# Maximum tokens for model responses (default: 2000)
MAX_TOKENS=2000

# Temperature for model responses (default: 0.3)
TEMPERATURE=0.3

# Confidence threshold for processing (default: 0.7)
CONFIDENCE_THRESHOLD=0.7

# =============================================================================
# LOGGING CONFIGURATION (OPTIONAL)
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Log format string
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# =============================================================================
# OPENTELEMETRY TRACING (OPTIONAL)
# =============================================================================
# OTLP endpoint for sending traces (default: http://localhost:4318)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Service instance identifier (default: local-instance)
HOSTNAME=local-instance

# Automatically set by main_with_tracing.py (do not modify):
# OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=true

# =============================================================================
# AZURE CLI AUTHENTICATION (ALTERNATIVE TO API KEYS)
# =============================================================================
# If you prefer to use Azure CLI authentication instead of API keys:
# 1. Run: az login
# 2. Run: az account set --subscription <your-subscription-id>
# 3. Leave the API key fields empty above
# 4. The system will automatically use Azure CLI credentials

# =============================================================================
# CONFIGURATION NOTES
# =============================================================================
# 1. REQUIRED: At minimum, you need AZURE_OPENAI_ENDPOINT and AZURE_OPENAI_DEPLOYMENT_NAME
# 2. OPTIONAL: Text Analytics and Document Intelligence enhance functionality but aren't required
# 3. FALLBACK: The system gracefully falls back to GPT-only processing when Azure services aren't configured
# 4. AUTHENTICATION: You can use either API keys or Azure CLI authentication
# 5. MODELS: Make sure your deployed model names match the configuration
# 6. TRACING: OpenTelemetry tracing is optional and requires a separate tracing backend
# =============================================================================
